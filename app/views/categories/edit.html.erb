<h1 class="text-center mt-4">Edit a category</h1>


<div class="container">
  <%= render "form" %>
  <%= link_to "[ Cancel and return to category ]", category_path(@category), class: "d-flex justify-content-center mb-3 text-info" %>
</div>